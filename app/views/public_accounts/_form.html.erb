<%= form_with(model: public_account, local: true) do |form| %>
  <% if public_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(public_account.errors.count, "error") %> prohibited this public_account from being saved:</h2>

      <ul>
      <% public_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :weixin_secret_key %>
    <%= form.text_field :weixin_secret_key, id: :public_account_weixin_secret_key %>
  </div>

  <div class="field">
    <%= form.label :weixin_token %>
    <%= form.text_field :weixin_token, id: :public_account_weixin_token %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
