<%= form_with(model: sub_menu, local: true) do |form| %>
  <% if sub_menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub_menu.errors.count, "error") %> prohibited this sub_menu from being saved:</h2>

      <ul>
      <% sub_menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :sub_menu_name %>
  </div>

  <div class="field">
    <%= form.label :is_show %>
    <%= form.check_box :is_show, id: :sub_menu_is_show %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
